RewriteEngine On

# Bypass API and real files
RewriteCond %{REQUEST_URI} ^/api(/|$) [OR]
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# SPA fallback to index.html
RewriteRule ^ index.html [L]
